name: Build And Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build DMG And ZIP
        run: |
          chmod +x ./scripts/make_app.sh ./scripts/make_dmg.sh
          ./scripts/make_app.sh
          mkdir -p dist/.ci-dmg-staging
          cp -R dist/NCMConverter.app dist/.ci-dmg-staging/NCMConverter.app
          ln -s /Applications dist/.ci-dmg-staging/Applications
          hdiutil create \
            -volname "NCMConverter" \
            -srcfolder dist/.ci-dmg-staging \
            -fs HFS+ \
            -format UDZO \
            -imagekey zlib-level=9 \
            -ov \
            dist/NCMConverter-unsigned.dmg
          rm -rf dist/.ci-dmg-staging

      - name: Collect Artifacts
        run: |
          mkdir -p release
          VERSION="${GITHUB_REF_NAME:-dev}"
          cp -f dist/NCMConverter-unsigned.zip "release/NCMConverter-${VERSION}.zip" || true
          cp -f dist/NCMConverter-unsigned.dmg "release/NCMConverter-${VERSION}.dmg" || true
          ls -la release

      - name: Upload Workflow Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: NCMConverter-release
          path: release/*
          if-no-files-found: error

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          overwrite_files: true
          files: |
            release/NCMConverter-${{ github.ref_name }}.zip
            release/NCMConverter-${{ github.ref_name }}.dmg
          generate_release_notes: true
